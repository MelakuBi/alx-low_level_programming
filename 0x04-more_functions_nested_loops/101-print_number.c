#include "main.h"
#include <stdio.h>
/**
 * print_number- to print intiger
 * @n: variable to accept number
 */
	void print_number(int n)
{
	int i;

/*	for (i 
	if (n < 0)
	_putchar(n * -1);
	else
	_putchar(n);
*/
	_putchar(n);
	_putchar(n);
	_putchar(n);
	_putchar(n);
	_putchar(n);
}
