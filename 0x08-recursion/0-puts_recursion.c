#include "main.h"
/**
 * _puts_recursion - return string with new line
 * @s: string
 * Return: nuthing
 */
	void _puts_recursion(char *s)
{
	int i, j = 0;

	while (*s)
	j++;
	for (i = 0; i < j; i++)
	_putchar(s[j]);
	_putchar('\n');
}
