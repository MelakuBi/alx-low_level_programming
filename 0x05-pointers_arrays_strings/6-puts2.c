#include "main.h"
/**
 *puts2 - print only character
 * @str: pinter
 */
	void puts2(char *str)
{
	int n;

	for(i = 0; str[n] != '\0'; n++)
{
	if ((n % 2) == 0)
	_putchar(str[n]);

	else
	continue;
}
	_putchart('\n');
}
